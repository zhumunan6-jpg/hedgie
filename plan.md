<proposed_plan>
# Hedgie 收入与存款功能分阶段执行计划

## 总体目标

将当前项目调整为两个主要界面：

- 第一个界面：收入、支出、累计存款、本月存款目标与进度。
- 第二个界面：收入与支出的月报展示。
- 删除月报中的交易记录窗口。
- 保留冬眠视图和隐藏的 Den 界面。
- 每完成一个阶段，先进行代码检查和功能验证，将结果同步到 `工作日志/工作日志.md`，完成 commit 和 push 后暂停；下一阶段只在新的对话中执行。

## 阶段一：数据结构、本地保存与旧数据清理

实现内容：

- 继续复用现有 `income` 数据字段，但将其改造成实际收入记录。
- 收入记录不设置类别，字段为：
  - 收入来源
  - 金额
  - 日期
  - 备注
  - 周期设置
- 增加本地数据持久化，确保刷新页面或重新打开应用后，收入、支出、目标和 Den 数据不会丢失。
- 将本地数据保存与现有云同步结合，云同步仍保持原有兼容逻辑。
- 删除 `Savings` 支出分类：
  - 删除分类选项、分类按钮、默认预算项目和相关计算逻辑。
  - 删除已有 `Savings` 分类的预算数据。
  - 删除已有 `Savings` 分类的历史记录和归档记录。
- 增加本月存款目标数据，按月份保存，例如 `2026-09` 对应一个目标金额。
- 旧版 `income` 数据中没有日期的有效收入，迁移到当前月份；金额为 0 的空收入项不迁移。

阶段完成处理：

1. 执行本地数据保存、刷新恢复和旧数据迁移检查。
2. 执行 `Savings` 分类清理检查，确认其他分类、周期账单和 Den 数据未被误删。
3. 执行内联 JavaScript 语法检查和必要的回归检查。
4. 将本阶段完成内容、验证结果和遗留问题写入 `工作日志/工作日志.md`。
5. 创建本阶段专属 commit 并 push 到远程仓库。
6. 阶段完成后暂停，不执行下一阶段，等待新的对话。

阶段通过标准：

- 数据不会因刷新而丢失。
- 收入记录可以正常保存和恢复。
- `Savings` 分类及其旧数据已被彻底清理。
- 其他已有功能没有回归问题。

## 阶段二：改造第一个界面

实现内容：

- 将“记录一笔支出”和“记录一笔收入”设计为视觉对称的两个窗口。
- 支出窗口保留现有功能：
  - 分类
  - 金额
  - 商家
  - 备注
  - 日期
  - 周期账单
- 收入窗口使用相同风格，但不显示类别选择：
  - 收入来源
  - 金额
  - 备注
  - 日期
  - 周期收入
- 在桌面端采用并排布局，在移动端自动上下排列。
- 删除首页当前的“某月份支出”指标窗口。
- 将原来的“剩余收入”改为“总存款”。
- 总存款按全部历史数据计算：

  `总存款 = 累计收入 - 累计支出`

- 新增“本月存款目标”设置窗口。
- 新增“本月存款进度”窗口，显示：
  - 本月实际存款
  - 本月目标金额
  - 完成百分比
  - 可视化进度条
- 本月存款按当前自然月计算：

  `本月存款 = 本月收入 - 本月支出`

- 目标未设置时显示空状态，不显示误导性的 0% 进度。
- 实际存款为负数时，进度条显示为 0%，同时保留负数金额和红色状态。

阶段完成处理：

1. 执行收入、支出、累计存款和本月存款的计算检查，覆盖正数、负数、跨月份和无目标状态。
2. 执行存款目标百分比和进度条边界检查，包括未达标、达标和超额完成。
3. 执行收入无分类检查，以及桌面端、移动端布局检查。
4. 执行收入编辑、删除、周期收入和支出原有功能回归检查。
5. 将本阶段完成内容、验证结果和遗留问题写入 `工作日志/工作日志.md`。
6. 创建本阶段专属 commit 并 push 到远程仓库。
7. 阶段完成后暂停，不执行下一阶段，等待新的对话。

阶段通过标准：

- 收入和支出可以独立记录。
- 总存款不受当前月份选择器影响。
- 本月存款和目标进度只按当前月份计算。
- 进度条、金额和百分比计算准确。
- 支出现有功能保持正常。

## 阶段三：改造第二个界面（月报）

实现内容：

- 保持当前整体颜色、字体、卡片和按钮风格。
- 优化月报页面的卡片排列和信息层级。
- 月报指标改为围绕收入、支出和存款展示：
  - 月收入
  - 实际支出
  - 本月存款
  - 本月存款目标及进度
- “按类别统计支出”继续保留。
- 删除月报底部“交易记录”窗口。
- 同时删除该窗口专用的：
  - HTML
  - 分类筛选逻辑
  - 交易记录渲染逻辑
  - 无用状态变量
  - 无用翻译文本
  - 无用 CSS
- 第一个界面的支出记录列表不删除，仍然保留编辑和删除能力。
- 冬眠视图暂时保留。
- 年度收入改为根据实际收入记录统计，避免继续使用旧的月收入乘以 12 的方式。
- 年度支出和分类统计继续保留，删除后的 `Savings` 分类不再参与显示。

阶段完成处理：

1. 执行多月份月报统计检查，确认收入、支出、本月存款和目标进度均按月份正确变化。
2. 执行月报分类支出统计检查。
3. 执行月报交易记录窗口及其专属代码清理检查。
4. 执行冬眠视图、首页支出记录列表和移动端布局回归检查。
5. 将本阶段完成内容、验证结果和遗留问题写入 `工作日志/工作日志.md`。
6. 创建本阶段专属 commit 并 push 到远程仓库。
7. 阶段完成后暂停，不执行下一阶段，等待新的对话。

阶段通过标准：

- 月报只负责展示，不承担交易编辑功能。
- 收入、支出和存款数据统计正确。
- 交易记录窗口及其专属代码已经清理干净。
- 冬眠视图仍可使用。
- 首页记录功能没有被误删。

## 阶段四：回归测试、代码清理与文档同步

实现内容：

- 全局搜索并清理残留的 `Savings` 分类逻辑。
- 全局搜索并清理月报交易记录窗口的残留代码。
- 清理失效的翻译文本、CSS、事件绑定和变量。
- 保持 `index.html` 单文件结构。
- 检查收入新字段是否正确进入云同步数据。
- 检查旧同步数据读取和新数据保存。
- 更新：
  - README
  - USER_GUIDE
  - 工作日志
- 不修改：
  - Den 功能主体
  - `server.js` 的接口行为
  - 周期支出账单
  - 商家记忆
  - 深色模式
  - PWA 安装逻辑
  - 钱包和投资逻辑

阶段完成处理：

1. 执行收入、支出、周期记录、本地恢复和存款目标的完整回归检查。
2. 执行深色模式、移动端滑动切换和 Den 功能回归检查。
3. 如已有云同步配置，执行收入和存款目标的同步检查。
4. 执行浏览器控制台错误检查，以及 `Savings` 分类和月报交易记录残留扫描。
5. 将本阶段完成内容、验证结果和遗留问题写入 `工作日志/工作日志.md`。
6. 创建本阶段专属 commit 并 push 到远程仓库。
7. 阶段完成后暂停，不执行下一阶段，等待新的对话。

最终验收标准：

- 收入不分类。
- 支出继续分类。
- 总存款按全部历史收支计算。
- 本月存款按当前月份计算。
- 本月存款目标和进度条正常。
- 月报不再显示交易记录窗口。
- 冬眠视图保留。
- 刷新页面不会丢失核心数据。
- 其他既有功能没有被破坏。

## 默认实现假设

- 使用现有 `income` 字段，不新增另一套收入主数据结构。
- 收入来源只是文字字段，不作为分类使用。
- `Savings` 分类及其历史数据按你的要求全部删除，不迁移、不保留。
- 本月目标按 `YYYY-MM` 保存，历史月份目标保留，但首页默认展示当前月份。
- 每个阶段只在一个独立对话中执行；阶段完成后同步工作日志、执行检查、commit、push，然后停止工作。下一阶段由你开启新的对话后再执行，不要求你进行人工验收。
</proposed_plan>
